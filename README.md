# mnist-digit-recognizer
Handwritten digit recognizer built with CNN (TensorFlow/Keras) trained on MNIST dataset achieving 98.93% test accuracy. Features real-time prediction via Gradio web app. Includes data preprocessing, model training, evaluation metrics, and live deployment on Hugging Face Spaces.
